---
title: '[HackerRank] Weather Observation Station 20'
description: sql problem
date: '2022-03-05T20:45:29+09:00'
tags: [SQL]
---

```sql
SELECT ROUND(LAT_N, 4)
FROM STATION AS S
WHERE (SELECT COUNT(*) FROM STATION WHERE LAT_N < S.LAT_N) = (SELECT COUNT(*) FROM STATION WHERE LAT_N > S.LAT_N)
```
